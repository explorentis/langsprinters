#languages structures:
# имя_папки="путь к запуску или строка компиляции", в строке компиляции используются:
!SOURCE_FILE! - заменяется на имя исходника
!EXEC_FILE! - имя собираемого бинарника (для компилируемых языков)
bash="/bin/bash !SOURCE_FILE!" #bash interpreter
python = "/usr/bin/python !SOURCE_FILE!" # i'm comment
и я коммент
 python="/usr/bin/python !SOURCE_FILE!"
php="/usr/bin/php !SOURCE_FILE!" # php interpreter
c="cc !SOURCE_FILE! -lm -Wall -o !EXEC_FILE!"
cpp="c++ -Wall -o !EXEC_FILE! !SOURCE_FILE!"
